# 安全说明

1. 订阅链接等同于访问凭据，不要公开粘贴到聊天群、截图或公开仓库。
2. 部署完成后立即修改 root 密码，优先改用 SSH 密钥并关闭 root 密码登录。
3. 如果订阅链接泄露，重新生成 `SUBSCRIPTION_TOKEN`，必要时重新生成 UUID 和 Reality 密钥。
4. 定期检查 VPS 面板的带宽、连接数和登录记录。
5. 只在自己拥有或明确获授权的服务器上部署和使用。
